/** @type {import('next').NextConfig} */
const nextConfig = {
  // Sur Netlify, on utilise le plugin @netlify/plugin-nextjs
  // Les API routes sont migrées vers netlify/functions/
  // Les redirections sont dans netlify.toml
}

module.exports = nextConfig
